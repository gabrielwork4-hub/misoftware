---
import { getCollection } from 'astro:content';
import EditorialQueuePage from '@/components/EditorialQueuePage.astro';
const entries = await getCollection('fila', ({ data }) => data.canonicalPath === '/ferramentas/n8n/');
const entry = entries[0];
if (!entry) throw new Error('Página editorial não encontrada: /ferramentas/n8n/');
---
<EditorialQueuePage entry={entry} />
