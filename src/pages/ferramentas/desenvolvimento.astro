---
import { getCollection } from 'astro:content';
import EditorialQueuePage from '@/components/EditorialQueuePage.astro';
const entries = await getCollection('fila', ({ data }) => data.canonicalPath === '/ferramentas/desenvolvimento/');
const entry = entries[0];
if (!entry) throw new Error('Página editorial não encontrada: /ferramentas/desenvolvimento/');
---
<EditorialQueuePage entry={entry} />
