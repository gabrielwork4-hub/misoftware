---
import { getCollection } from 'astro:content';
import EditorialQueuePage from '@/components/EditorialQueuePage.astro';
const entries = await getCollection('fila', ({ data }) => data.canonicalPath === '/ferramentas/pesquisa/');
const entry = entries[0];
if (!entry) throw new Error('Página editorial não encontrada: /ferramentas/pesquisa/');
---
<EditorialQueuePage entry={entry} />
