---
import { getCollection } from 'astro:content';
import EditorialQueuePage from '@/components/EditorialQueuePage.astro';
const entries = await getCollection('fila', ({ data }) => data.canonicalPath === '/ferramentas/produtividade/');
const entry = entries[0];
if (!entry) throw new Error('Página editorial não encontrada: /ferramentas/produtividade/');
---
<EditorialQueuePage entry={entry} />
